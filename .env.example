# ===========================
# Market Data Platform — Repo A
# ===========================
# Copy to .env and fill in values.
# All services read from root .env (via dotenv or Railway inject).

# --- Common ---
NODE_ENV=development
APP_VERSION=0.0.1
LOG_LEVEL=info

# --- PostgreSQL (Railway managed) ---
DATABASE_URL=postgresql://user:pass@localhost:5432/marketdata

# --- S3-Compatible Bucket (Railway Storage) ---
BUCKET_ENDPOINT=https://your-bucket.railway.internal
BUCKET_ACCESS_KEY=
BUCKET_SECRET_KEY=
BUCKET_NAME=tick-log

# --- Service ports (local dev) ---
API_SERVICE_PORT=3001
WS_HUB_PORT=3002
MD_COLLECTOR_PORT=3003
UI_PORT=5173

# --- WS Hub (internal, used by md-collector) ---
WS_HUB_INTERNAL_URL=ws://localhost:3002

# --- API Service (used by UI) ---
API_SERVICE_URL=http://localhost:3001
